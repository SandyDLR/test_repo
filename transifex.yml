filters:
  - filter_type: dynamic
    file_format: YML
    source_language: es
    source_files_expression: 'packs/benefit/<dir>/es.yml'
    translation_files_expression: 'packs/benefit/<dir>/<lang>.yml'

settings:
  language_mapping:
    pt_BR: pt
